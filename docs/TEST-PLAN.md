# Test plan - pointer, battery, scroll, enable/disable

Hardware on a Windows 10/11 PC with Magic Tray running. Record tray version (footer), Windows build, and `diagnose-and-recover.ps1` output after any failure.

Catalog PIDs: v1 `030D`, v3 `0323`. Skip rows for a mouse that is not present.

## A. Magic Mouse v3 (USB-C, `0323`)

Setup: paired over Bluetooth, USB-C unplugged, Test Mode on, Memory integrity off, KMDF bound.

| # | Action | Pointer | Battery | Scroll |
|---|---|---|---|---|
| A1 | Move / click | Cursor moves | - | - |
| A2 | Tray **Refresh Now** | - | Percent, Input 0x90 COL02 | - |
| A3 | Two-finger swipe | - | - | Content scrolls |
| A4 | Plug USB-C, charge 1 min or more | May switch to USB HID (no wheel) | Percent may jump (0 to 100) | Wheel may die on USB. Expected. |
| A5 | Unplug, wait 10 s | Bluetooth pointer | Percent again | **Wheel must return** |
| A6 | If A5 wheel dead | Tray problem row -> auto-fix (see D), or `diagnose-and-recover.ps1 -Repair` | Unchanged | Wheel returns; the service named in the device's `LowerFilters` (on this PC `MagicMouseDriver204Scroll`) is STATE=RUNNING |
| A7 | Tray **Enabled on this PC** off then on (do **not** Remove in Settings) | Stops, then starts | Follows | Follows |

Pass A5 or A6. Fail only if the wheel is dead while the **bound** filter service reports RUNNING, or if the tray badges KMDF while the **bound** service is STOPPED. The first of those shapes now has **two** diagnoses, and one read of `DEVPKEY_Device_Stack` on the live BTHENUM instance separates them: the bound filter **absent** from the stack is fault A, registered and loaded but **not attached** to this mouse's stack, which is D18 to D21 below; the bound filter **present** in the stack with the wheel still dead is fault B, the mouse never received the Apple multi-touch enable feature report `{0xF1, 0x02, 0x01}` on COL01, which is D22 to D24 below and is owned by the driver package's F1 watcher, not by the tray (see "Which dead-wheel fault is this" in `docs/ENABLE-DISABLE.md`). Do not judge by `sc query MagicMouseDriver`: on this PC that older service is installed but not bound, and it is Stopped even when scrolling works. `sc start` on a lower filter always exits 31 and means nothing.

## B. Magic Mouse v1 (AA, `030D`)

Setup: paired, Boot Camp `applewirelessmouse` bound - by Apple's INF or by the sbagirici manual service install, the same Apple-signed binary either way. Test Mode not required on either route.

| # | Action | Pointer | Battery | Scroll |
|---|---|---|---|---|
| B1 | Move / click | Cursor moves | - | - |
| B2 | **Refresh Now** | - | Percent, unified Feature 0x47 | - |
| B3 | Two-finger swipe | - | - | Content scrolls |
| B4 | Tray **Enabled on this PC** off | Stops | Row may hide (poller skips disabled PID) | Stops |
| B5 | Same checkbox on | Starts | Percent | Scroll |
| B6 | **Remove** in Bluetooth settings, then tray Enable | Must **fail** with "not present" and offer Settings | - | - |
| B7 | Underside switch off/on until LED blinks -> Add device | Pairs | Percent after pair | Scroll after Boot Camp bind |

Do not expect B5 to work after B6. Pairing is Settings, not Enable.

## C. App - enable/disable and Diagnostics

| # | Action | Expected |
|---|---|---|
| C1 | Enable/disable confirm -> Cancel | Checkbox unchanged. No UAC. |
| C2 | Enable/disable -> UAC Cancel | Checkbox unchanged. `enabled_*` unchanged. |
| C3 | Enable with no PnP instance | "not present" dialog. OK opens Bluetooth settings. `sidecar=no-instances`. |
| C4 | Diagnostics -> **Run diagnose-and-recover.ps1** | Capture window. No repair. File `diagnose-and-recover-*.txt` next to cwd / `%APPDATA%\MagicMouseTray`. |
| C5 | Diagnostics -> capture-state / diagnose-driver / snapshot | Still present. capture-state remains 0323-only. |

## D. In-app repair (problem row, guided dialog, auto-fix)

The zero-PowerShell path. Reproduce each state on hardware, then work the row the tray offers.

| # | Check |
|---|---|
| D1 | Healthy stack (bound filter RUNNING, device live, row enabled): the menu shows **No problems found**. No toast, no dialog, nothing to click. |
| D1b | **False-positive regression.** On the reference PC the 0323 device binds `MagicMouseDriver204Scroll` (RUNNING) while an older, unbound `MagicMouseDriver` service is installed and Stopped. Confirm the tray still reports **No problems found**: `sc query MagicMouseDriver204Scroll` RUNNING, `sc query MagicMouseDriver` STOPPED, wheel scrolls. A stopped same-family service that is not in the device's `LowerFilters` must produce no finding and no toast. |
| D2 | v3 `0323` with the **bound** filter service not running while still named in `LowerFilters` (unplug after a USB-C charge, or `sc stop MagicMouseDriver204Scroll` - stop the name the device actually binds): a **problem row appears at the top of the tray menu** naming that scroll filter. **Diagnostics -> Check for problems now** finds the same thing on demand. |
| D3 | Open that row: the guided dialog states the PID, what was found, the cause, and what the fix will do **before** anything runs. No device is touched until you press the action button. |
| D4 | Press the fix (one UAC prompt): the app runs `pnputil /restart-device` on the live **BTHENUM** instances only - no `sc start` in `debug.log` - the bound filter service comes back to STATE=RUNNING because PnP reloaded it with the stack, and the **wheel scrolls again**. Pointer and battery keep working throughout. |
| D5 | Verify the fix left the rest alone: still paired, USB / `HID\VID_` phantoms untouched, Bluetooth radio on, no `FLIP:NoFilter` in `debug.log`. |
| D6 | Blocked filter, and only when it really is blocked: after the BTHENUM restart the bound filter is still not running **and** test signing is off (`bcdedit` / `SystemStartOptions` has no `TESTSIGNING`) or Memory integrity is on. The dialog reports the block and gives the prerequisites - **Test Mode on, Memory integrity off, reboot**. With test signing already on and HVCI off, the same still-not-running state must be reported as a plain failure, **not** as a signing problem. `sc start` exiting 31 is never used as evidence: a PnP lower filter is not startable through the SCM. |
| D7 | v1 `030D` with zero PnP and zero Enum instances (disable, then **Remove device** in Settings, so `enabled_030d` is in `config.ini`): the row explains the pairing is gone and lists the pairing steps (underside switch off/on until the LED blinks, Settings -> Add device, then re-check **Enabled on this PC**). The fix is **not** offered as automatic and **no** `pnputil /enable-device` runs - confirm in `debug.log`. |
| D8 | USB charge-cable phantoms only (no live BTHENUM) on a PID this PC has an `enabled_<pid>` entry for: the row says the mouse is not connected over Bluetooth and that the phantoms must be left alone. Nothing is enabled or restarted. |
| D9 | `enabled_<pid>=false` while nodes are live: the row offers the automatic re-enable, and after it runs `config.ini` reads `enabled_<pid>=true` with the device working. |
| D10 | Toast behaviour: a detected problem toasts **once per problem per session**. Re-open the menu, refresh, or leave it running - no repeat toast for the same problem. Restart the tray with the problem still present: one toast again. |
| D11 | **Ownership regression (22 findings).** On a PC that owns two Apple devices, the tray must report at most those two and never the rest of the catalog: with both healthy the menu reads **No problems found**, and with one fault exactly one row appears. Confirm in `debug.log` that a menu open writes a `REPAIR_SNAPSHOT` line only for PIDs with live BTHENUM nodes, USB phantoms, or an `enabled_<pid>` entry - not one line per catalog PID. A catalog keyboard or trackpad this PC has never had produces no snapshot line and no finding. The bug this pins reported `REPAIR_FINDINGS count=22` on a two-device PC. |
| D12 | **Two scroll drivers registered.** On the reference PC state (device key `LowerFilters=[MagicMouseDriver]` with that service Stopped, instance key `LowerFilters=[MagicMouseDriver204Scroll]` with that service Running, both under `HKLM\SYSTEM\CurrentControlSet\Enum\BTHENUM`): the tray reports one problem row for `0323` naming the conflict while the working filter is RUNNING. `debug.log` shows the `REPAIR_SNAPSHOT` line listing both candidates. |
| D13 | Open that row: the dialog names **both** drivers before anything runs - the leftover it will remove (`MagicMouseDriver`) and the working one it keeps (`MagicMouseDriver204Scroll`) - plus one admin prompt, no unpairing. Cancel touches nothing: re-read the registry and both `LowerFilters` values are unchanged. |
| D14 | Press the fix (one UAC prompt): afterwards exactly **one** family filter name is registered across the device key and the instance key (`MagicMouseDriver204Scroll` only), the leftover is gone, and the **wheel scrolls again**. `debug.log` records the previous `LowerFilters` value verbatim before the write. |
| D15 | After D14 the battery percent returns on a later poll (**Refresh Now** once or twice): the COL02 vendor report stops coming back zeroed `[90 00 00]` and a percent appears again. Pointer and clicks work throughout. |
| D16 | Correctly installed PC (one vendor filter name on the stack, service RUNNING): **No conflict reported.** The menu reads **No problems found**, no toast, no dialog - a single-filter stack must never be diagnosed as a conflict. |
| D17 | **Refusal check.** With only one family filter registered (or with the leftover already removed), the repair must refuse to remove the last remaining filter: no `LowerFilters` value is emptied, no service key or `.sys` is deleted, non-family filters (`mouhid`, `HidBth`) stay in place, no `FLIP:NoFilter`, the radio is untouched, and USB / `HID\VID_` phantoms are not modified. Running the row in that state reports that nothing needed fixing. |
| D18 | **Wheel dead after a restart while the driver reads correct.** v3 `0323` with live BTHENUM instances, a bound filter name, the filter package present, `sc query <bound filter>` STATE=RUNNING, and that filter **absent** from `DEVPKEY_Device_Stack` (restart Windows and let PnP rebuild the stack from its cached HID layout; confirm the state with `Get-PnpDeviceProperty -InstanceId <bthenum instance> -KeyName DEVPKEY_Device_Stack`): the tray reports one row for `0323` titled **Scroll wheel is dead - the driver is not attached to this mouse** and offers the automatic restart. `debug.log` shows the stack read for that PID - `REPAIR_STACK pid=0323 bound=MagicMouseDriver204Scroll in_stack=false instances=1 readable=1` - and after the fix the `pnputil /restart-device` on the live **BTHENUM** instances only: no `sc start`, no `LowerFilters` write. |
| D19 | **Unknown is never a fault.** The same shape but with the property unreadable (no BTHENUM instance id resolves, or every property read fails): `debug.log` reads `in_stack=unknown` on the `REPAIR_STACK` line, or a `REPAIR_STACK_FAILED pid=0323 bound=... err=...` line, and the menu still reads **No problems found**. No toast, no dialog, and nothing is restarted - confirm no repair ran. Unknown must not be treated as "not attached". |
| D20 | **Honest verification after the restart.** For this fault `sc query` was already RUNNING before the repair, so the service state proves nothing: after pressing the fix in D18 the tray re-reads `DEVPKEY_Device_Stack` and logs a second `REPAIR_STACK` line for the PID. Filter now in the stack (`in_stack=true`): success is reported and the **wheel scrolls again**. Filter still absent (`in_stack=false`): the tray must **not** claim success - it says Windows kept the cached layout for this mouse and shows the re-pair steps (Bluetooth & devices -> Remove device, power the mouse off and on, Add device). |
| D21 | **Healthy-mouse regression for the new rule.** A working mouse whose bound filter **is** in `DEVPKEY_Device_Stack` (wheel scrolls): **No problems found**, no toast, no restart. An attached filter must never raise the new finding, whatever `LowerFilters` and the service state look like - this rule must not nag a working PC. |
| D22 | **Differential: filter attached and the wheel still dead is NOT a tray fault.** v3 `0323` with live BTHENUM instances, the bound filter name present, the package present, `sc query <bound filter>` STATE=RUNNING, and that filter **present** in `DEVPKEY_Device_Stack` (reference PC reads `\Driver\HidBth \| \Driver\MagicMouseDriver204Scroll \| \Driver\BthEnum`), `DEVPKEY_Device_ProblemCode` 0, both HID children OK - while the wheel does not scroll: the tray must report **NO problem for `0323`** (menu reads **No problems found**, no toast, no dialog, nothing restarted; `debug.log` logs `in_stack=true`). **That is a PASS.** A tray finding in this state would be a **false positive** - every read is byte-identical to the same PC after scroll was restored (measured 2026-09-15). Redirect the tester to the driver repo's F1 watcher (`v2-kmdf-driver/scripts/mm-auto-f1-watcher.ps1` in `LesleyMurfin/magic-mouse-v3-windows-fix`): the mouse never got `{0xF1, 0x02, 0x01}` on COL01. |
| D23 | **A zeroed `0x90` pull is never a fault.** With a usermode `GET_REPORT(Input, 0x90)` pull on COL02 returning `[90 00 00]` on every poll, the tray must raise **nothing** from it: no problem row, no toast, no repair, and `MOUSE_BATTERY_ZERO` in `debug.log` keeps its "no battery report sent yet - idle mouse" reading. Cite the measurement: the same `[90 00 00]` came back 10/10 over 5 s on a **dead** wheel and 10/10 again on the **same PC with multitouch confirmed flowing**, so those zeros cannot discriminate a fault. The percent arrives as a **pushed** input report instead (healthy series 72/71/61/54/47/42 percent in `debug.log` up to 2026-09-15 01:58:49). |
| D24 | **Cross-repo expectation, owned by the driver repo.** On a PC using the KMDF `0323` driver, reboot Windows and touch nothing: multitouch must be live with **no manual step** (the F1 watcher's startup reconciliation, since at boot the mouse is already enumerated before an arrival-only subscription can see it). Verify by the **wheel scrolling** and the **battery percent reappearing on the next poll** (**Refresh Now** once or twice). A failure here is a driver-package bug, not a tray bug, and it must still leave the tray at **No problems found** per D22. Listed here only so the tray tester knows the expected post-reboot state; Magic Tray does not send F1 and adding that is a non-goal. |
| D25 | **Pointer child gone while the Bluetooth parent is live.** v3 `0323` with live BTHENUM instances and the PID's Bluetooth-transport COL01 HID child registered under `HKLM\SYSTEM\CurrentControlSet\Enum\HID` but not resolving as present (disable just the COL01 HID child in Device Manager; confirm `CM_Locate_DevNodeW` cannot resolve it, e.g. `Get-PnpDevice -InstanceId <bluetooth col01 child>` reporting it not present): the tray reports **exactly one** row for `0323` naming the pointer child, and offers the automatic fix. `debug.log` shows the pointer probe result for the PID and, after the fix, the `pnputil /restart-device` on the live **BTHENUM** instances only - no `sc start`, no `pnputil` against a `USB\` or `HID\VID_` node - and the pointer works again. |
| D26 | **Unknown pointer evidence is never a fault.** The same PID with no Bluetooth COL01 child key at all (mouse powered off / unpaired but still `enabled_<pid>` in config), or with the probe failing: the pointer probe logs unknown and the menu raises **nothing from it** - no pointer row, no toast, no dialog, nothing restarted. A powered-off or out-of-range mouse must not be reported as a broken pointer. |
| D27 | **REJECTED SIGNAL regression: `Diag\LastAclReceived` = 9 raises NOTHING.** On a **healthy** v3 `0323` (filter bound, RUNNING, present in `DEVPKEY_Device_Stack`, wheel scrolling), sample `HKLM\SYSTEM\CurrentControlSet\Services\<bound filter>\Diag` repeatedly until `LastAclReceived` reads **9** while scroll is still working - measured on the reference PC at 2.5 s intervals: `23/142271`, `23/142513`, **`9`**`/142614`, `23/142827` (`LastAclReceived`/`AclTranslateCount`), the counter advancing straight through the `9`. In that state the tray must report **No problems found** for `0323`: no scroll finding, no toast, no dialog, nothing elevated, and no `{0xF1, 0x02, 0x01}` write anywhere in `debug.log`. A finding here would be a false positive on a working mouse. |
| D28 | **Counter movement is the only positive proof, and stillness is unknown.** Use the mouse (two-finger swipe) across two poll samples: `AclTranslateCount` (or `Rid12Count` when the first is absent) advances and the device row's scroll line reads as **working**. Then leave the mouse untouched, and separately test a PC where the `Diag` key or the value is missing: the counter does not advance / cannot be read, the scroll line falls back to the neutral not-verified wording, and **no finding, no toast and no repair** appear in either case. Not advancing must never be rendered or logged as a fault - an idle mouse and a broken one are the same read. |
| D29 | **Scroll help is user-initiated.** On a v3 `0323` whose bound filter is registered, RUNNING and **in** `DEVPKEY_Device_Stack`, the device row offers **Scroll is not working**; it is absent or disabled where that precondition does not hold (unbound, service stopped, filter not attached - those states are the automatic rules D2 / D12 / D18 instead). Clicking it opens the guidance dialog: power the mouse off and on with the underside switch as the immediate step, install or restart the driver package's multitouch watcher as the permanent fix, and the watcher state the tray read from `C:\ProgramData\MagicMouseDriver\auto-f1-watcher.log` (installed / last heartbeat / last F1 result, or unknown for each). Assert the **absence** in `debug.log`: no UAC, no `pnputil`, no `schtasks`, no script launch, and no F1 / `{0xF1, 0x02, 0x01}` write by the tray. Cancel or close touches nothing. |
| D30 | **Battery: one finding, and the deliberate silences.** A **connected Magic Keyboard** whose percent reads `-2` (SDP cache unpatched, or the patch erased by a re-pair; `KB_BATTERY_BLOCKED` / `KB_FEATURE_BLOCKED` in `debug.log`): exactly one row for that keyboard, routed to the existing **Fix battery reads** offer (`scripts/kbd-patch-cachedservices.ps1 -Mac <keyboard MAC>`, one elevated run, user-confirmed), and after it runs the percent appears. Then a **mouse** reading `-2`, `-3` or `-1` (leave `0323` idle until the tooltip says `Battery unavailable` / `No reading`): **no row, no toast, no repair** for any of the three. That silence is intentional - the percent is a pushed report, an idle mouse legitimately sends none, and the zeroed `0x90` pull is already rejected per D23 - so a finding there would nag every healthy PC daily. |
| D31 | **Per-device capability lines, with unknown rendered as unknown.** Each device row shows its pointer, scroll and battery state, and anything the tray has no evidence for reads as unknown / not verified rather than as working or broken. A Magic **Keyboard** and a Magic **Trackpad** row show **no scroll line at all** (no vendor scroll filter and no multitouch counter applies to them), and neither produces a scroll finding or a **Scroll is not working** item. |
| D32 | **Guidance-only findings do not nag.** With a guidance-only state present (D7 zero instances, D8 phantoms only, or the D29 dialog left open/cancelled), leave the tray running across several poll ticks and open the menu repeatedly: the row stays, and the toast fires **once per problem per session** exactly as D10 requires - no repeat toast per tick, no repeated dialog, and no repair is ever attempted for a guidance-only finding. |
| D33 | **Transient install churn raises nothing.** While the driver package is being installed or the device restarted (`pnputil /restart-device`, or an install that briefly leaves the service Stopped, the filter out of `DEVPKEY_Device_Stack`, two rival filter names registered, or the COL01 pointer child absent), leave the tray running: `debug.log` shows `REPAIR_FINDINGS raw=N confirmed=0 pending=N held=<pid>:<Problem>` and **no toast, no dialog, no repair offer** appears while the state lasts under the hold window. The repo's own numbers set the floor: the post-restart script polls up to 10 s for the filter to reappear (`DeviceRepair.cs:195`, `:472`) and `diagnose-and-recover.ps1` sleeps 3 s after a restart. A fault that vanishes inside the window must leave no trace beyond the `held=` lines - a repair offered against an in-flight install would fight it. |
| D34 | **Boot and resume re-check confirms a standing fault without a menu open.** Start the tray (or resume the PC from sleep) with a genuine fault present and then **do not touch the tray**: `debug.log` shows `HEALTH_SETTLE trigger=startup attempt=1/3`, `2/3`, `3/3` (resume logs `trigger=resume`), the finding goes `pending` then `confirmed`, and the toast fires on its own - measured on the reference PC 2026-09-15 18:43:12 / :29 / :46, confirmed at attempt 2. The same spacing gives `MultitouchAdvancing` two counter samples 17 s apart instead of three inside one startup second (`43986 -> 44167 -> 44656`, `advancing=unknown` then `true`). The sequence is finite: after attempt 3 no further re-check occurs until a poll tick, a menu open, or the next resume - assert no fourth `HEALTH_SETTLE` line and no change to the poller's cadence. |
| D35 | **Driver advice in every state.** On a `0323` mouse, drive each driver state in turn - KMDF bound, patched Apple bound, stock Windows bound, nothing bound - and open the **Driver** submenu each time: it always shows exactly one advice line. On KMDF the line says affirmatively that this is the recommended driver and there is nothing to change. On the patched Apple driver, on stock Windows and with nothing bound it names KMDF as the recommendation and states the concrete cost of staying put (the capability the current state gives up). The line is never blank, never truncated to a bare driver name, and never absent once a driver is bound - that silence was the bug. |
| D36 | **Per-driver expectations are labelled as predictions.** The submenu lists one row per driver option beneath a header that states these are what each driver **would** give, not a reading of the live device. The patched Apple row reads as scroll **or** battery and never both at once. A Magic Keyboard and a Magic Trackpad show a single stock row on which scroll reads as not applicable and is **not** worded as broken, missing or failing. None of these rows is confusable with the observed `Pointer:` / `Scroll:` / `Battery:` capability lines on the device row itself (D31): different wording, and the prediction rows never change when the live device state changes. |
| D37 | **The battery flip, happy path.** On a `0323` bound to the patched Apple driver, click the read-battery item: exactly one confirmation dialog appears first, naming the brief pause in pointer or scroll, the single administrator approval, and the guaranteed automatic restore. Accept it: exactly **one** UAC prompt for the whole cycle, a battery percent is shown, and afterwards the scroll wheel scrolls again with the tray reporting the mouse verified back in scroll mode (Mode B). `debug.log` shows the `MODE_FLIP` phases in order and records the verbatim previous `LowerFilters` value before it is changed. |
| D38 | **The flip always restores.** Run the flip and kill the tray mid-cycle (Task Manager, during the pause), then separately run it again and let the reading fail (mouse switched off after the flip): in both cases the elevated script still writes back the **recorded previous** `LowerFilters` value and restarts the device, and the scroll wheel works afterwards. A failed reading is reported as a failed reading only - never as a failed or skipped restore, and never as a success. If the tray was killed before the restore was confirmed, the next launch offers the one-click restore item, and taking it restores the wheel; declining it leaves the offer available and touches nothing. |
| D39 | **The flip is refused where it does not apply.** On a KMDF-bound `0323`, on a v1 / v2 mouse (`030d` / `0269`), on a Magic Keyboard and on a Magic Trackpad, the read-battery flip item is **absent** from the menu. Reach the underlying call anyway (the item force-enabled, or the API invoked directly) and it returns without changing anything: no UAC prompt, no registry write, no `pnputil`, nothing restarted, and no `MODE_FLIP` phase beyond the refusal in `debug.log`. |
| D40 | **Configuration facts never nag a healthy PC.** With Test Mode on, Memory integrity off and the driver package present, the configuration section reads as all checks passed - nothing warning-shaped, no yellow or red wording, no offer to change anything. Turn Memory integrity **on** (or Test Mode off) while a self-signed driver is bound: exactly **one** blocking fact appears, naming that exact setting and what it blocks, and the tray changes no security setting itself (assert no `bcdedit`, no HVCI registry write in `debug.log`). A device bound to stock Windows produces **no** configuration fact at all. A setting the tray cannot read renders as unknown and never produces a blocking fact. With a genuine device fault present at the same time, the device fault sorts **above** the configuration section. |
| D41 | **Pointer evidence on both mouse shapes.** A v3 `0323` that exposes `&COL01`, `&COL02` and a collection-less parent under `HKLM\SYSTEM\CurrentControlSet\Enum\HID` must resolve its pointer from **COL01**: `debug.log` reads `DEVICE_DIAG_POINTER pid=0323 live=true ... shape=col01`, the collection-less aggregate seen and ignored (`col01=1 nocol=1`). A v1/v2 single-collection mouse, where Windows writes no `&COL0x` suffix at all, must resolve from its sole node - reference PC 2026-09-15, pointer and scroll both working: `DEVICE_DIAG_POINTER pid=030d live=true keys=1 present=1 shape=sole col01=0 nocol=1`. Then a PID with **two rival collection-less candidates and no COL01** (the same PID under both Apple VID spellings, or a stale pairing record beside the live one): `shape=ambiguous`, `live=unknown`, `keys=0 present=0`, and the menu raises **nothing** from it - no pointer row, no toast, no dialog, nothing restarted, no `pnputil` in `debug.log`. The regression this pins was caught live on the reference PC on 2026-09-15: the working v3 read `shape=ambiguous col01=1 nocol=1` and so `live=unknown`, while its pointer was in use. Ambiguity is a refusal to choose, never a fault, and COL02 is never pointer evidence. |
| D42 | **Signing facts follow the driver file, not the PID.** A v1/v2 on the Apple-signed `applewirelessmouse.sys` (self-issued `WDKTestCert` primary signature plus two valid `CN=Microsoft Windows Hardware Compatibility Publisher` signatures) with Test Mode **off**: no Test Mode and no Memory integrity fact appears for that device, and `debug.log` reads `SYSTEM_CONFIG kind=MagicMouseV1 pid=030d ... selfsigned=false ... blocking=0`. A **self-signed** bound driver (v3 KMDF, `CN=MagicMouseFix`, self-issued) with Test Mode off, or with Memory integrity on: exactly **one** blocking fact, naming that setting, `selfsigned=true blocking=1` in the same line, and the tray changes no security setting itself (no `bcdedit`, no HVCI write in `debug.log`). A driver file whose signature cannot be read - no embedded signature, file absent, or the read failing (`SYSTEM_CONFIG_SIGNATURE_FAILED`) - logs `selfsigned=unknown`, produces **no** blocking fact, and never states or implies the driver is self-signed. Changing which install route put the Apple binary on the PC must change none of this: the reading is of the file, not of the PID and not of the installer. |

## E. Scripted diagnose and recovery (elevated CLI fallback)

```powershell
.\scripts\diagnose-and-recover.ps1              # capture
.\scripts\diagnose-and-recover.ps1 -Repair      # elevated; BTHENUM restart only
.\scripts\diagnose-and-recover.ps1 -DevicePid 0323
```

| # | Check |
|---|---|
| E1 | Lists catalog PIDs that have PnP nodes or `enabled_*` in config. Includes Error/Unknown/phantom, not only Status=OK. |
| E2 | Reports the filter service each live device actually binds (from its `LowerFilters`) with STATE, plus the KMDF-family and `applewirelessmouse` services it finds installed. An installed-but-unbound service is labelled as such, not as a fault. |
| E3 | Tags BTHENUM vs USB/phantom. Diagnoses a **bound** filter that is not running, zero instances for configured PIDs, config vs PnP, USB leftovers after charge. A stopped service that no device binds is not diagnosed as the scroll fault. |
| E4 | `-Repair` without admin exits 1. With admin, restarts **only** live BTHENUM. Leaves USB phantoms. Never unpairs. Never `FLIP:NoFilter`. |
| E5 | Zero instances: pairing steps, no `pnputil /enable-device`. |

## User verification - 2026-09-06 incident

Leave unchecked until you confirm on this PC.

- [ ] v3: pointer after unplug
- [ ] v3: battery percent (not stuck 0% while charged)
- [ ] v3: **wheel** after unplug, or after `diagnose-and-recover.ps1 -Repair`
- [ ] v1: paired via Settings (underside switch) - tray Enable was the wrong tool after Remove
- [ ] v1: pointer, battery, scroll
- [ ] tray Enabled on this PC off/on on a **still-paired** mouse
- [ ] Diagnostics capture script ran; txt file kept
