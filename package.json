{
  "name": "magic-drivers-donation-system",
  "version": "0.1.0",
  "private": true,
  "description": "Community-funded driver donation system: Stripe Checkout, webhooks, Resend email receipts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test __tests__/**/*.test.js",
    "test:watch": "node --watch --test __tests__/**/*.test.js"
  },
  "dependencies": {
    "@sentry/node": "^7.91.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260000.0",
    "wrangler": "^3.60.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
